---
interface Props {
  listTitle: string;
  listItems: {
    title: string;
    journal: string;
    time: string;
  }[];
}
const {listTitle, listItems} = Astro.props
---

<h2 class='text-3xl font-bold mb-8'>Recent Publications</h2>
		<div class='space-y-6'>
			{
				listItems.map(item => (
					<div class='p-6 w-[300px] h-[400px] rounded-lg text-base-content bg-base-200'>
						<h3 class='text-xl font-semibold mb-2'>{item.title}</h3>
						<p >
							{item.journal} • {item.time}
						</p>
					</div>
				))
			}
		</div>
